<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <ul class="nav navbar-nav">
                <li class="{% if active_page is defined and active_page == 'admin_home' %}active{% endif %}">
                    <a class="navbar-brand" href="{{ path('admin_page') }}">{{ 'navigation.home'|trans }}</a>
                    <p class="navbar-text">&#124;</p>
                </li>
                <li class="{% if active_page is defined and active_page == 'registration' %}active{% endif %}">
                    <a class="navbar-brand" href="{{ path('registration') }}">{{ 'navigation.register'|trans }}</a>
                    <p class="navbar-text">&#124;</p>
                </li>
                <li class="{% if active_page is defined and active_page == 'users' %}active{% endif %}">
                    <a class="navbar-brand" href="{{ path('user_list') }}">{{ 'navigation.users'|trans }}</a>
                    <p class="navbar-text">&#124;</p>
                </li>
                <li class="{% if active_page is defined and active_page == 'recurring_schedule' %}active{% endif %}">
                    <a class="navbar-brand" href="{{ path('jmose_command_scheduler_list') }}">{{ 'navigation.recurring_schedule'|trans }}</a>
                    <p class="navbar-text">&#124;</p>
                </li>
                <li class="{% if active_page is defined and active_page == 'onetime_schedule' %}active{% endif %}">
                    <a class="navbar-brand" href="#">{{ 'navigation.onetime_schedule'|trans }}</a>
                </li>
            </ul>
        </div>
    </div>
</div>
