{% extends "base.html.twig" %}
{% set active_page = 'users' %}
{% block title %}{{ 'user.registration.title'|trans }}{% endblock title %}
{% block pageTitle %}{{ 'user.registration.title'|trans }}{% endblock pageTitle %}

{% block content %}
    <div class="col-lg-6 col-sm-12">
        {{ form(form) }}
        <div class="row">
            <a href="{{ path('user_list') }}" class="btn btn-default btn-lg" role="button">Cancel</a>
            {{ form_widget( form.submitButton, {'attr': {'class': 'btn btn-success btn-lg pull-right'} } ) }}
        </div>
    </div>
{% endblock content %}
